{% extends 'base.html.twig' %}

{% block section %}{% endblock %}

{% block body %}
    {# 12.-------------------------------Login:  Modif page twig login---------------------------------- #}
    {#<div class="container"></div>#}

    <h1>MA PAGE DE LOGIN</h1>
    <section>
        {% if error %}
            <div>{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}
        
        <div class="row">
            <div class="col-md-offset-2 col-md-4">
                <div class="form-group">
                    <form action="{{ path('login') }}" method="post">
                        <label for="username">Email:</label>
                        <input type="text" class="form-control" id="username" name="_username" value="{{ last_username }}" />
                    
                        <label for="password">Mot de Passe:</label>
                        <input type="password" class="form-control" id="password" name="_password" />
    {# 12.-----------------------------------------------------------------------------------------------------------------------#}


    {# 13.---------------------Login:  Ajouter <input type="hidden" name="_csrf_token"... ---------------------------------------#}
                        <!-- SECURITE CSRF A RAJOUTER -->
                        <input type="hidden" class="form-control" name="_csrf_token" value="{{ csrf_token('authenticate') }}">                   
    {# 13.-------------------------------------Se rendre dans secutityController-------------------------------------------------#}                    
                        
                        {#    
                            If you want to control the URL the user
                            is redirected to on success (more details below)
                            <input type="hidden" name="_target_path" value="/account" />
                        #}
                    
                        <button type="submit" class="btn btn-danger" >login</button>
                    </form>
                    <p><a href="{{ path('password-revovery') }}" >Mot de passe oublié</a></p>
                </div>      
            </div>
    </div> 
    </section>

    {#</div>#}
{% endblock %}
