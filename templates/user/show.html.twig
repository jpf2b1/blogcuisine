{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}
{% block section %}{% endblock %}
{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ user.id }}</td>
            </tr>
            <tr>
                <th>Username</th>
                <td>{{ user.username }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ user.password }}</td>
            </tr>
            {#<tr>#}
                {#<th>Roles</th>#}
                {#<td>{{ user.roles }}</td>#}
            {#</tr>#}
        </tbody>
    </table>

    <button class="btn btn-success"><a href="{{ path('user_index') }}">back to list</a></button>

    <button class="btn btn-success"><a href="{{ path('user_edit', {'id': user.id}) }}">edit</a></button>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
